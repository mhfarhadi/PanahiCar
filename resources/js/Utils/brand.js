export const BRAND_FA = 'پناهی';
export const BRAND_EN = 'Panahi Car';
export const THEME_KEY = 'panahi_theme';

export const pageTitle = (section) => (section ? `${section} | ${BRAND_EN}` : BRAND_EN);
